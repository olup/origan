# @origan/nitro-preset

Publishable Nitro preset for emitting Origan deployment manifests.

## Status
- Preset export is a stub.
- CLI is a placeholder for future manifest generation.

## Usage (planned)
```ts
import { preset } from "@origan/nitro-preset";

export default defineNitroConfig({
  preset: preset(),
});
```
